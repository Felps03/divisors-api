const _isPrime = (number) => {
  return number;
};

const _divisors = (number) => {
  return number;
};

const divisorsAndPrime = (number) => {
  return number;
};

module.exports = {
  divisorsAndPrime,
};
